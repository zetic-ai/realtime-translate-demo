# The Melange SDK ships a large class set; dex merging runs out of the Gradle
# daemon's 512 MB default heap without this.
org.gradle.jvmargs=-Xmx4g -Dfile.encoding=UTF-8
android.useAndroidX=true
kotlin.code.style=official
